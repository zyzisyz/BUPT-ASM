;data
DATA SEGMENT
    MSG DB 'hello world','$'
DATA ENDS

;stack
STACK SEGMENT STACK'STACK'
    DB 100H DUP(?)
STACK ENDS

;code
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA,SS:STACK
START PROC FAR
    PUSH DS
    XOR AX,AX
    PUSH AX
    MOV AX,DATA
    MOV DS,AX
    MOV AH,09H
    MOV DX,OFFSET MSG
    INT   21H
    RET
START ENDP
CODE ENDS
    END START
